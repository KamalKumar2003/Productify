<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
</head>
<style>
    table, tr, td, th {
        border: 1px solid black;
    }
</style>
<body>
    <form method="GET" action="/products/new">
        <button>Add Item</button>
    </form>
    <table>
        <tr>
            <th>id</th>
            <th>item</th>
            <th>Price</th>
            <th>Stock</th>
            <th>Supplier</th>
        </tr>
        <% for(item of items){ %>
            <tr>
                <td><%=item.id %></td>
                <td><%=item.item %></td>
                <td><%=item.Price %></td>
                <td><%=item.Stock %></td>
                <td><%=item.Supplier %></td>
                <td>
                <form method="GET" action="/products/<%=item.id%>/edit">
                    <button>Edit Details</button>
                </form>
                </td>
                <td>
                    <form method="GET" action="/products/<%= item.id%>/delete">
                        <button>Delete</button>
                    </form>
                </td>
            </tr>
        <%}%>
    </table>
</body>
</html> -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products - Shop Management</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            height: 100%;
            overflow-y: auto;
            overflow-x: hidden;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: #e8ecf1;
            min-height: 100vh;
            padding: 20px 20px 80px 20px;
        }

        .navbar {
            background: white;
            padding: 20px 40px;
            border-radius: 20px;
            margin-bottom: 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 2px 15px rgba(0, 0, 0, 0.1);
        }

        .nav-brand {
            font-size: 1.8rem;
            font-weight: 800;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .nav-links {
            display: flex;
            gap: 25px;
        }

        .nav-link {
            color: #4a5568;
            text-decoration: none;
            font-weight: 600;
            padding: 8px 16px;
            border-radius: 10px;
            transition: all 0.3s ease;
        }

        .nav-link:hover {
            color: #667eea;
            background: #f7fafc;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 40px;
            flex-wrap: wrap;
            gap: 20px;
        }

        h1 {
            font-size: 2.5rem;
            color: #1a202c;
            font-weight: 800;
        }

        .btn-primary {
            padding: 14px 30px;
            border-radius: 12px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-decoration: none;
            font-weight: 700;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
            transition: all 0.3s ease;
            display: inline-block;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.5);
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 30px;
            margin-bottom: 40px;
        }

        .product-card {
            background: white;
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 2px 15px rgba(0, 0, 0, 0.08);
            transition: all 0.3s ease;
            border-top: 5px solid #667eea;
            height: auto;
        }

        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
        }

        .product-id {
            display: inline-block;
            background: #e6f2ff;
            color: #2c5aa0;
            padding: 6px 16px;
            border-radius: 8px;
            font-size: 0.85rem;
            font-weight: 700;
            margin-bottom: 15px;
        }

        .product-details {
            margin-bottom: 25px;
        }

        .detail-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            border-bottom: 2px solid #f0f4f8;
        }

        .detail-row:last-child {
            border-bottom: none;
        }

        .detail-label {
            color: #718096;
            font-size: 1rem;
            font-weight: 600;
        }

        .detail-value {
            color: #1a202c;
            font-weight: 700;
            font-size: 1.1rem;
            text-align: right;
            max-width: 60%;
            word-wrap: break-word;
        }

        .item-name {
            color: #667eea;
            font-size: 1.2rem;
            font-weight: 700;
        }

        .price {
            color: #48bb78;
            font-size: 1.4rem;
        }

        .stock {
            background: #c6f6d5;
            color: #22543d;
            padding: 6px 16px;
            border-radius: 8px;
            font-weight: 700;
        }

        .stock.low {
            background: #fed7d7;
            color: #742a2a;
        }

        .product-actions {
            display: flex;
            gap: 12px;
        }

        .btn-sm {
            flex: 1;
            padding: 12px 20px;
            border-radius: 10px;
            text-decoration: none;
            text-align: center;
            font-weight: 700;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            display: block;
        }

        .btn-edit {
            background: #4299e1;
            color: white;
        }

        .btn-edit:hover {
            background: #3182ce;
            transform: translateY(-2px);
        }

        .btn-delete {
            background: #fc8181;
            color: white;
        }

        .btn-delete:hover {
            background: #f56565;
            transform: translateY(-2px);
        }

        .empty-state {
            text-align: center;
            padding: 100px 40px;
            background: white;
            border-radius: 25px;
            box-shadow: 0 2px 15px rgba(0, 0, 0, 0.08);
        }

        .empty-icon {
            font-size: 5rem;
            margin-bottom: 25px;
        }

        .empty-text {
            font-size: 1.4rem;
            color: #718096;
            margin-bottom: 30px;
            font-weight: 600;
        }

        @media (max-width: 768px) {
            body {
                padding: 15px 15px 60px 15px;
            }

            .navbar {
                padding: 15px 20px;
                flex-direction: column;
                gap: 15px;
            }

            .header {
                flex-direction: column;
                align-items: flex-start;
            }

            h1 {
                font-size: 2rem;
            }

            .products-grid {
                grid-template-columns: 1fr;
            }

            .detail-value {
                max-width: 50%;
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="nav-brand">🏪 Shop Manager</div>
        <div class="nav-links">
            <a href="/" class="nav-link">Home</a>
            <a href="/products" class="nav-link">Products</a>
        </div>
    </nav>

    <div class="container">
        <div class="header">
            <h1>Product Inventory</h1>
            <a href="/products/new" class="btn-primary">+ Add New Product</a>
        </div>

        <% if (items.length === 0) { %>
            <div class="empty-state">
                <div class="empty-icon">📦</div>
                <div class="empty-text">No products found in your inventory</div>
                <a href="/products/new" class="btn-primary">Add Your First Product</a>
            </div>
        <% } else { %>
            <div class="products-grid">
                <% for (let item of items) { %>
                    <div class="product-card">
                        <span class="product-id">ID: <%= item.id %></span>
                        
                        <div class="product-details">
                            <div class="detail-row">
                                <span class="detail-label">Item</span>
                                <span class="detail-value item-name"><%= item.item %></span>
                            </div>
                            <div class="detail-row">
                                <span class="detail-label">Price</span>
                                <span class="detail-value price">₹<%= item.Price %></span>
                            </div>
                            <div class="detail-row">
                                <span class="detail-label">Stock</span>
                                <span class="stock <%= parseInt(item.Stock) < 10 ? 'low' : '' %>">
                                    <%= item.Stock %> units
                                </span>
                            </div>
                            <div class="detail-row">
                                <span class="detail-label">Supplier</span>
                                <span class="detail-value"><%= item.Supplier %></span>
                            </div>
                        </div>

                        <div class="product-actions">
                            <a href="/products/<%= item.id %>/edit" class="btn-sm btn-edit">✏️ Edit</a>
                            <a href="/products/<%= item.id %>/delete" class="btn-sm btn-delete" 
                               onclick="return confirm('Are you sure you want to delete this product?')">🗑️ Delete</a>
                        </div>
                    </div>
                <% } %>
            </div>
        <% } %>
    </div>
</body>
</html>